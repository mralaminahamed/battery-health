<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Loopback only: the privileged tier reaches the on-device adb daemon on 127.0.0.1.
         Android enforces INTERNET at socket creation via the inet group, and loopback is
         not exempt, so staying on-device does not avoid it. The app makes no outbound
         network requests; PrivilegedShellLoopbackTest asserts that mechanically. -->
    <!--
      Signature|privileged|development. A normal app can never be granted this by a user
      tap, but the "development" flag means adb can grant it directly:

          adb shell pm grant com.alaminahamed.batteryhealth android.permission.BATTERY_STATS

      Declared so that grant has something to attach to. Ungranted it does nothing at all
      and the app behaves exactly as before, so this is inert on every device that has not
      run the command.
    -->
    <uses-permission android:name="android.permission.BATTERY_STATS" />

    <!--
      Signature|privileged|development, same tier as BATTERY_STATS above and grantable the
      same way:

          adb shell pm grant com.alaminahamed.batteryhealth android.permission.DUMP

      Declared to test one specific question: whether it opens a real per-app battery
      attribution path via a "dumpsys batterystats" checkin dump, which BATTERY_STATS alone
      does not reach. See the permissions-visibility report for the verdict; this permission
      stays declared only because that experiment came back positive.
    -->
    <uses-permission android:name="android.permission.DUMP" />

    <!--
      Appop-gated: declaring this grants nothing by itself. The user must flip it on from
      Settings, Apps, Special access, Usage access, which this app can only deep-link to
      (Settings.ACTION_USAGE_ACCESS_SETTINGS): there is no dialog. See
      AppOpPermissionState for how "held" is actually determined, which is not the same
      question as whether this permission is merely declared.
    -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <application
        android:name=".BatteryHealthApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BatteryHealth">
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BatteryHealth.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".sampling.ChargeRecorderService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Samples battery charge counters every five seconds while charging to measure full capacity, which cannot be derived from 15-minute periodic work" />
        </service>

        <receiver
            android:name=".sampling.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>