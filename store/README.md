# Play Store assets

Icon, feature graphic, listing copy and screenshots for the Play Console listing.

The **Apps screen screenshot is deliberately absent**: it lists a real device's installed
applications, so it lives in the private `battery-health-store` repository along with the
original unsanitised parser fixture and the tester email list. Fetch it from there when
updating the listing.
